{% extends 'base.html.twig' %}

{% block title %}Intervention{% endblock %}

{% block body %}
    <h1>Intervention sur l'instrument n° {{ intervention.instrument.numSerie}}</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ intervention.id }}</td>
            </tr>
            <tr>
                <th>Type d'instrument</th>
                <td>{{ intervention.instrument.type.libelle}}</td>
            </tr>
            <tr>
                <th>DateDebut</th>
                <td>{{ intervention.dateDebut ? intervention.dateDebut|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>DateFin</th>
                <td>{{ intervention.dateFin ? intervention.dateFin|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ intervention.description }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ intervention.prix }}</td>
            </tr>
            <tr>
                <th>Quotite</th>
                <td>{{ intervention.quotite }}</td>
            </tr>
            
                
            
            

        </tbody>
    </table>

    <a href="{{ path('app_intervention_edit', {'id': intervention.id}) }}">edit</a>
    <a href="{{ path('app_intervention_index') }}">back to list</a>

    

    {{ include('intervention/_delete_form.html.twig') }}
    
{% endblock %}
