{% extends 'base.html.twig' %}

{% block title %}Paiment{% endblock %}

{% block body %}
    <h1>Paiment</h1>

    <table class="table">
        <tbody>
        {% if is_granted('ROLE_ADMIN') %}
            <tr>
                <th>Id</th>
                <td>{{ paiment.id }}</td>
            </tr>
        {% endif %}
            <tr>
                <th>Montant</th>
                <td>{{ paiment.montant }}</td>
            </tr>
            <tr>
                <th>DatePaiment</th>
                <td>{{ paiment.datePaiment ? paiment.datePaiment|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>


    
    <a class="btn btn-primary" href="{{ path('app_paiment_index') }}">Revenir à la liste</a>

    {% if is_granted('ROLE_ADMIN') %}
        <a  class="btn btn-primary" href="{{ path('app_paiment_edit', {'id': paiment.id}) }}">Editer</a>
        
        {{ include('paiment/_delete_form.html.twig') }}
    {% endif %}
{% endblock %}
