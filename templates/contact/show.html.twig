{% extends 'base.html.twig' %}

{% block title %}Contact{% endblock %}

{% block body %}
    <h1>Message : </h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Nom</th>
                <td>{{ contact.Nom }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ contact.Email }}</td>
            </tr>
            <tr>
                <th>Message</th>
                <td>{{ contact.Message }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-primary" href="{{ path('app_contact_index') }}">Revenir à la liste</a>

    <a class="btn btn-primary" href="{{ path('app_contact_edit', {'id': contact.id}) }}">Editer</a>
    
    {{ include('contact/_delete_form.html.twig') }}
{% endblock %}
