{% extends 'base.html.twig' %}

{% block title %}Accueil{% endblock %}

{% block body %}
    
    {# --- ADMINISTRATEUR --- #}
    {% if is_granted('ROLE_ADMIN') %}
        <div class="info-card">
            <h2>Bienvenue {{ app.user.prenom }} {{ app.user.nom }}</h2>
            <br>
            <p>Connecté en tant que : {{ app.user.nom }}</p>
            <p>ID de l'utilisateur : {{ app.user.id }}</p>
            <p>Rôle : Administrateur</p>
            
            <div class="btn-group">
                <a href="{{ path('app_paiment_index') }}" class="btn btn-outline-primary">💳 Gestion des paiements</a>
                <a href="{{ path('app_instrument_index') }}" class="btn btn-outline-primary">🎸 Gestion des instruments</a>
                <a href="{{ path('app_inscription_cours') }}" class="btn btn-outline-primary">Gestion des cours</a>
                
                {# NOUVEAU BOUTON : Voir les demandes de contact/inscription #}
                <a href="{{ path('app_contact_cours_index') }}" class="btn btn-outline-primary">📩 Demandes d'inscription</a>
            </div>
        </div>
        
    {# --- GESTIONNAIRE --- #}
    {% elseif is_granted('ROLE_GESTIONNAIRE') %}
        <div class="info-card">
            <h2>Bienvenue {{ app.user.prenom }} {{ app.user.nom }}</h2>
            <br>
            <p>Connecté en tant que gestionnaire</p>
            
            <div class="btn-group">
                <a href="{{ path('app_paiment_index') }}" class="btn btn-outline-primary">💳 Liste des paiements</a>
                <a href="{{ path('app_instrument_index') }}" class="btn btn-outline-primary">🎸 Parc instruments</a>
                <a href="{{ path('app_inscription_cours') }}" class="btn btn-outline-primary">Gestion des cours</a>

                {# NOUVEAU BOUTON : Voir les demandes de contact/inscription #}
                <a href="{{ path('app_contact_cours_index') }}" class="btn btn-outline-primary">📩 Demandes d'inscription</a>
            </div>
        </div>
        
    {# --- RESPONSABLE --- #}
    {% elseif is_granted('ROLE_RESPONSABLE') %}
        <div class="info-card">
            <h2>Bienvenue {{ app.user.prenom }} {{ app.user.nom }}</h2>
            <br>
            
            <div class="btn-group">
                <a href="{{ path('app_paiment_index') }}" class="btn btn-outline-primary">💳 Liste des paiements</a>
                <a href="{{ path('app_instrument_index') }}" class="btn btn-outline-primary">🎸 Parc instruments</a>
            </div>
        </div>
        
        <div class="info-card">
            <h3>👨‍🎓 Mes élèves</h3>
            <br>
            
            {# Bouton pour s'inscrire (Formulaire de contact) #}
            <a href="{{ path('app_contact_cours_new') }}" class="btn btn-primary">S'inscrire à un cours</a>
        </div>
        
    {# --- ELEVE --- #}
    {% elseif is_granted('ROLE_ELEVE') %}
        <div class="info-card">
            <h1>Bienvenue {{ app.user.prenom }} {{ app.user.nom }}</h1>
            <br>
            <br>
            
            <div class="btn-group">
                <a href="{{ path('app_paiment_index') }}" class="btn btn-outline-primary">💳 Liste des paiements</a>
                <a href="{{ path('app_instrument_index') }}" class="btn btn-outline-primary">🎸 Parc instruments</a>
            </div>
        </div>
        
        <div class="info-card">
            <br>
            <h3>📚 Mes cours</h3>
            {% if coursList is not empty %}
                <div class="dashboard-table"> 
                    <table>
                        <thead>
                            <tr>
                                <th>Nom du cours</th>
                                <th>Date</th>
                            </tr>
                        </thead>
                        <tbody>
                            {% for cour in coursList %}
                                <tr onclick="window.location='{{ path('app_cours_show', {'id': cour.id}) }}'"
                                    style="cursor: pointer;">
                                    <td>{{ cour.libelle }}</td>
                                    <td>{{ cour.heureDebut|date('d/m/Y à H:i') }}</td>
                                </tr>
                            {% endfor %}
                        </tbody>
                    </table>
                </div>
                <br>
            {% else %}
                <p>Aucun cours inscrit.</p>
                <br>
            {% endif %}
            
            {# Bouton pour s'inscrire (Formulaire de contact) #}
            <a href="{{ path('app_contact_cours_new') }}" class="btn btn-primary">S'inscrire à un cours</a>
            
        </div>
        
        <br><br><br><br><br><br>
        
    {% else %}
        <div class="info-card">
            <h2>⚠️ Erreur</h2>
            <p>Rôle non reconnu</p>
        </div>
    {% endif %}
    
{% endblock %}