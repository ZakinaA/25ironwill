{% extends 'base.html.twig' %}

{% block title %}Edit Responsable{% endblock %}

{% block body %}
    <h1>Edit {{ responsable.prenom }} {{ responsable.nom }}</h1>

    {{ include('responsable/_form.html.twig', {'button_label': 'Mette à jour'}) }}

    <a href="{{ path('app_responsable_index') }}" class="btn btn-info">
            Retour à la liste
    </a>

    {{ include('responsable/_delete_form.html.twig') }}
{% endblock %}
